import FormBuilder from "./form-builder";

// form builder
type Props = {};

const ComplexExemple = ({}: Props) => {
  return (
    <div>
      <h2>------------ Exemple n°2 - Form Builder ------------</h2>
      <FormBuilder />
    </div>
  );
};

export default ComplexExemple;
